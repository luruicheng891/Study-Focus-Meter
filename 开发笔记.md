# OV2640_Demo 开发笔记

---

## 一、项目元信息

| 项目 | 内容 |
|------|------|
| 产品名称 | OV2640 摄像头采集显示 Demo |
| 固件版本 | V1.0 |
| MCU | STM32H723ZGT6 (Cortex-M7, 550MHz) |
| 摄像头模块 | OV2640 (SCCB 接口, 最大 1600×1200) |
| LCD 模块 | ILI9341 (240×320, SPI 接口, RGB565) |

### 开发环境

| 项目 | 版本/说明 |
|------|-----------|
| IDE | Keil MDK-ARM V5 |
| 编译器 | ARM Compiler V5 (armcc) |
| 固件库 | STM32H7xx HAL Driver |
| RTOS | FreeRTOS Kernel V10.3.1 + CMSIS-RTOS V2 |
| GUI 库 | LVGL v8.3.11 |
| 调试器 | ST-Link / J-Link |

### 关键文档链接

- 数据手册: [STM32H723ZGT6 Datasheet](https://www.st.com/resource/en/datasheet/stm32h723zg.pdf)
- 参考手册: [RM0468 - STM32H723/733 Reference Manual](https://www.st.com/resource/en/reference_manual/rm0468-stm32h723733-stm32h725735-and-stm32h730-value-line-advanced-armbased-32bit-mcus-stmicroelectronics.pdf)
- OV2640 数据手册: OV2640 Camera Module Datasheet (Omnivision)
- ILI9341 数据手册: ILI9341 TFT LCD Controller Datasheet

---

## 二、硬件抽象层笔记

### 2.1 时钟树配置

```
HSE = 25MHz (外部高速晶振，假设板载为 25MHz)

PLL1 配置:
  PLLM = 10  → VCO 输入 = 25MHz / 10 = 2.5MHz
  PLLN = 220 → VCO 输出 = 2.5MHz × 220 = 550MHz
  PLLP = 1   → SYSCLK = 550MHz / 1 = 550MHz
  PLLQ = 2   → PLL1Q = 275MHz
  PLLR = 2   → PLL1R = 275MHz

总线时钟:
  SYSCLK   = 550 MHz
  AHB/HCLK = 550 / 2 = 275 MHz
  APB1     = 275 / 2 = 137.5 MHz
  APB2     = 275 / 2 = 137.5 MHz
  APB3     = 275 / 2 = 137.5 MHz
  APB4     = 275 / 2 = 137.5 MHz

电压调节: VOS0 (最高性能模式)
Flash 等待周期: FLASH_LATENCY_3
```

**注意事项:**
- 使用 `PWR_LDO_SUPPLY` 供电模式
- 必须等待 `PWR_FLAG_VOSRDY` 后才能配置 PLL
- ICache 和 DCache 均已开启 (`SCB_EnableICache()` / `SCB_EnableDCache()`)

### 2.2 MPU 配置

| 区域 | 基地址 | 大小 | 属性 |
|------|--------|------|------|
| Region 0 | 0x24000000 (AXI SRAM) | 512KB | Full Access, Bufferable, Cacheable, Shareable |

> 用途: 摄像头 DMA 帧缓冲区放置在 AXI SRAM，配置为可缓存+可共享，确保 DMA 与 CPU 数据一致性。

### 2.3 引脚分配表

#### DCMI 接口 (OV2640 摄像头)

| 引脚 | 信号名 | 类型 | 备注 |
|------|--------|------|------|
| PA6 | DCMI_PIXCLK | AF13 推挽 | 像素时钟 |
| PG9 | DCMI_VSYNC | AF13 推挽 | 垂直同步 |
| PC6 | DCMI_D0 | AF13 推挽 | 数据位 0 |
| PC7 | DCMI_D1 | AF13 推挽 | 数据位 1 |
| PG10 | DCMI_D2 | AF13 推挽 | 数据位 2 (注释标注) |
| PG11 | DCMI_D3 | AF13 推挽 | 数据位 3 (注释标注) |
| PE4 | DCMI_D4 | AF13 推挽 | 数据位 4 |
| PD3 | DCMI_D5 | AF13 推挽 | 数据位 5 |
| PE5 | DCMI_D6 | AF13 推挽 | 数据位 6 |
| PE6 | DCMI_D7 | AF13 推挽 | 数据位 7 |
| PF13 | OV2640_PWDN | GPIO 推挽输出 | 电源控制 (高=休眠) |

#### SCCB 接口 (软件模拟 I2C)

| 引脚 | 信号名 | 类型 | 备注 |
|------|--------|------|------|
| PF14 | SCCB_SCL | GPIO 开漏输出 | I2C 时钟, 软件模拟 |
| PF15 | SCCB_SDA | GPIO 开漏输出 | I2C 数据, 软件模拟 |

> OV2640 设备地址: 0x60

#### SPI1 (LCD ILI9341)

| 引脚 | 信号名 | 类型 | 备注 |
|------|--------|------|------|
| PB3 | SPI1_SCK | AF5 推挽 | SPI 时钟 |
| PB5 | SPI1_MOSI | AF5 推挽 | SPI 数据输出 |
| PD0 | LCD_CS | GPIO 推挽输出 | 片选 (低有效) |
| PD1 | LCD_DC | GPIO 推挽输出 | 数据/命令选择 |
| PD2 | LCD_RST | GPIO 推挽输出 | 硬件复位 (低有效) |
| PB4 | LCD_LED | GPIO 推挽输出 | 背光控制 (高=亮) |

#### USART1 (调试串口)

| 引脚 | 信号名 | 类型 | 备注 |
|------|--------|------|------|
| PA9 | USART1_TX | AF 推挽 | 波特率 921600 |
| PA10 | USART1_RX | AF 输入 | 波特率 921600 |

#### LED

| 引脚 | 信号名 | 类型 | 备注 |
|------|--------|------|------|
| PG7 | LED1 | GPIO 推挽输出 | 低电平点亮 |

软件模拟I2C

| 引脚 | 信号名 | 类型         | 备注      |
| ---- | ------ | ------------ | --------- |
| PB8  | SCL    | GPIO推挽输出 | I2C时钟线 |
| PB9  | SDA    | GPIO推挽输出 | I2C数据线 |



### 2.4 外设配置参数

#### SPI1 (LCD) + DMA

```c
// SPI1 配置
Mode              = SPI_MODE_MASTER
Direction         = SPI_DIRECTION_2LINES_TXONLY  // 仅发送
DataSize          = SPI_DATASIZE_8BIT
CLKPolarity       = SPI_POLARITY_LOW   (CPOL=0)
CLKPhase          = SPI_PHASE_1EDGE    (CPHA=0)
BaudRatePrescaler = SPI_BAUDRATEPRESCALER_8
// APB2=137.5MHz, 分频8 → SPI CLK ≈ 17.2MHz
FirstBit          = SPI_FIRSTBIT_MSB

// DMA1_Stream0 配置 (SPI1_TX)
Request           = DMA_REQUEST_SPI1_TX (38)
Direction         = DMA_MEMORY_TO_PERIPH
PeriphDataAlignment = DMA_PDATAALIGN_BYTE
MemDataAlignment    = DMA_MDATAALIGN_BYTE
Mode              = DMA_NORMAL
Priority          = DMA_PRIORITY_HIGH
FIFOMode          = DMA_FIFOMODE_DISABLE
```

> ⚠️ DMA 缓冲区必须在 AXI SRAM (0x24000000+), 不能在 DTCM (0x20000000)!

#### DCMI

```c
SynchroMode  = DCMI_SYNCHRO_HARDWARE   // 硬件同步
PCKPolarity  = DCMI_PCKPOLARITY_RISING  // 上升沿采样
VSPolarity   = DCMI_VSPOLARITY_LOW      // VS 低电平有效
HSPolarity   = DCMI_HSPOLARITY_LOW      // HS 低电平有效
CaptureRate  = DCMI_CR_ALL_FRAME        // 捕获所有帧
数据宽度      = 8 位
```

#### FreeRTOS 关键配置

```c
configTICK_RATE_HZ        = 1000        // 1ms 节拍
configTOTAL_HEAP_SIZE     = 15360       // 15KB 堆
configMAX_PRIORITIES      = 56
configMINIMAL_STACK_SIZE  = 128 words   // 512 bytes
configTIMER_TASK_STACK_DEPTH = 256 words
Heap 实现: heap_4.c
```

#### 任务列表

| 任务名 | 栈大小 | 优先级 | 功能 |
|--------|--------|--------|------|
| Camera_task | 1024 words (4KB) | osPriorityNormal | 摄像头采集 + LCD 刷屏 |

---

## 三、驱动与中间件笔记

### 3.1 OV2640 摄像头驱动

- 像素格式: RGB565
- 采集分辨率: 400×300 (OV2640 输出)
- 显示分辨率: 240×240 (DCMI 硬件裁剪后)
- DMA 缓冲区: 位于 AXI SRAM (0x24000000)
- DMA 缓冲区大小: `240 × 240 × 2 / 4 = 28800` (32 位字)
- 传输模式: DMA 连续模式 (Continuous)

**帧处理流程:**
1. DCMI 中断回调设置 `DCMI_FrameState = 1`
2. Camera_task 检测到标志后:
   - 调用 `SCB_InvalidateDCache_by_Addr()` 使 DCache 无效化
   - 调用 `LCD_DrawBuffer()` 将帧数据刷新到 LCD

### 3.2 LCD ILI9341 驱动 (SPI1 + DMA)

- 通信方式: SPI1 (仅发送, 无 MISO) + DMA1_Stream0 加速
- 屏幕尺寸: 240×320
- 颜色格式: RGB565 (16bit)
- SPI 时钟: APB2/8 ≈ 17.2MHz
- DMA 通道: DMA1_Stream0, DMAMUX request = DMA_REQUEST_SPI1_TX (38)

**DMA 传输架构:**
- Ping-pong 双缓冲: `lcd_dma_buf[2][1024]` (每个 buffer 512 像素)
- 缓冲区位置: AXI SRAM 0x24040000 (DTCM 不可被 DMA 访问!)
- 传输前调用 `SCB_CleanDCache_by_Addr()` 刷新 DCache
- DMA 完成中断 → `HAL_SPI_TxCpltCallback()` → 清除 busy 标志
- `LCD_DMA_Wait()` 带超时保护，防止 DMA 异常时死循环

**批量传输流程 (LCD_DrawBuffer):**
1. 设置窗口 (阻塞 SPI 发送命令字节)
2. 切换 DC 为数据模式，保持 CS 低
3. 循环: CPU 在 buf[idx] 做字节序转换 → 等待上一次 DMA 完成 → 启动 DMA 发送 → 切换 idx
4. 等待最后一次 DMA 完成 → 释放 CS

**中断配置:**
- DMA1_Stream0_IRQHandler → `HAL_DMA_IRQHandler(&hdma_spi1_tx)`
- SPI1_IRQHandler → `HAL_SPI_IRQHandler(&hlcd_spi)` (错误处理)

### 3.3 I2C 软件模拟驱动

- 实现方式: GPIO bit-bang (非硬件 I2C)
- 引脚: PB8 (SCL) / PB9 (SDA), 开漏 + 内部上拉
- 速率: ~100kHz (通过 `I2C_DELAY_US = 5` 控制)
- 源文件: `Communication/I2C.c`

**API 接口:**
```c
void    I2C_Sim_Init(void);                                              // GPIO 初始化
uint8_t I2C_WriteData(uint8_t dev_addr, uint8_t reg_addr, uint8_t *data, uint16_t len);
uint8_t I2C_ReadData(uint8_t dev_addr, uint8_t reg_addr, uint8_t *buffer, uint16_t len);
```

> 注意: `dev_addr` 为 7 位地址，函数内部自动左移并添加 R/W 位。

### 3.4 MPU6050 六轴传感器驱动

- 通信接口: 软件 I2C (PB8/PB9)
- 设备地址: 0x68
- 采样率: 100Hz
- 陀螺仪量程: ±2000°/s
- 加速度计量程: ±16g
- 数字低通滤波: DLPF 带宽 5Hz
- 源文件: `Communication/MPU6050.c`

**初始化流程:**
1. 写 `PWR_MGMT_1 = 0x01` → 退出睡眠，选择 X 轴陀螺仪 PLL 作为时钟源
2. 写 `PWR_MGMT_2 = 0x00` → 使能所有轴
3. 写 `SMPLRT_DIV = 0x09` → 采样率分频 (100Hz)
4. 写 `CONFIG = 0x06` → DLPF 配置
5. 写 `GYRO_CONFIG = 0x18` → 陀螺仪 ±2000°/s
6. 写 `ACCEL_CONFIG = 0x18` → 加速度计 ±16g

**数据读取:**
- `MPU6050_GetData()` — 逐寄存器读取 (12 次 I2C 事务)
- `MPU6050_GetDataFast()` — 从 0x3B 连续读取 14 字节 (1 次 I2C 事务，推荐使用)

**寄存器映射 (连续读取 buffer[0..13]):**

| 偏移 | 内容 |
|------|------|
| [0:1] | AccX (高:低) |
| [2:3] | AccY |
| [4:5] | AccZ |
| [6:7] | Temperature (未使用) |
| [8:9] | GyroX |
| [10:11] | GyroY |
| [12:13] | GyroZ |

### 3.5 LVGL 集成

- 版本: v8.3.11
- 颜色深度: 16 bit (RGB565)
- 显示端口: `lv_port_disp.c` (位于 `LVGL/examples/porting/`)
- 刷屏回调: 调用 `LCD_SetWindows()` + `LCD_WriteDataBuffer()`

### 3.6 资源占用

| 资源 | 使用情况 | 备注 |
|------|----------|------|
| AXI SRAM (0x24000000) | 摄像头帧缓冲 ~115.2KB | 240×240×2 bytes |
| AXI SRAM (0x24040000) | LCD DMA 双缓冲 2KB | ping-pong, 32 字节对齐 |
| DTCM (0x20000000) | 栈、堆、普通变量 | DMA 不可访问! |
| FreeRTOS Heap | 15KB (heap_4) | 动态任务/队列分配 |
| Camera_task 栈 | 4KB | 含 LCD 刷屏操作 |
| DMA1_Stream0 | SPI1 TX (LCD) | DMAMUX request 38 |
| DMA2_Stream7 | DCMI RX (摄像头) | 循环模式 |

---

## 四、工程问题与解决日志

### 问题 1: LVGL lv_draw_sw 链接错误

**现象:** 链接阶段报 `Undefined symbol lv_draw_sw_init_ctx / lv_draw_sw_deinit_ctx / lv_draw_sw_transform`

**定位:** 检查编译输出，发现 `lv_draw_sw.c` 等文件未出现在编译列表中。

**原因:** Keil 工程文件 (`.uvprojx`) 中 LVGL 源文件路径错误。工程使用扁平路径 `..\LVGL\src\lv_draw_sw.c`，但 LVGL v8 实际目录结构为 `LVGL/src/draw/sw/lv_draw_sw.c`。

**解决:** 修正 `.uvprojx` 中所有 `lv_draw_sw_*.c` 的 `<FilePath>` 为:
```xml
<FilePath>..\LVGL\src\draw\sw\lv_draw_sw.c</FilePath>
```

**验证:** 修正后文件可被编译器找到并编译。

---

### 问题 2: LCD_WriteDataBuffer 未定义

**现象:** 链接报错 `Undefined symbol LCD_WriteDataBuffer`

**定位:** `lv_port_disp.c` 中调用了 `LCD_WriteDataBuffer()`，但该函数在 LCD 驱动中不存在。

**原因:** LVGL 显示端口移植时新增了该函数调用，但忘记在 `lcd.c` 中实现。

**解决:** 在 `LCD/lcd.c` 中实现 `LCD_WriteDataBuffer()`，功能为在窗口已设置的前提下批量写入像素数据:
```c
void LCD_WriteDataBuffer(uint16_t *pBuf, uint32_t pixel_count)
{
    // 发送 GRAM 写命令后，分批通过 SPI 发送像素数据
}
```
同时在 `lcd.h` 中添加函数声明。

**验证:** 编译通过，无链接错误。

---

### 问题 3: lv_conf.h 找不到 (lv_draw_sw 编译错误)

**现象:** `lv_draw_sw.c` 编译时报 `cannot open source input file "../../lv_conf.h"`

**定位:** `lv_conf_internal.h` 使用相对路径 `#include "../../lv_conf.h"`，该路径相对于 `LVGL/src/` 目录解析为 LVGL 上级目录。但从 `LVGL/src/draw/sw/` 编译时，相对路径解析错误。

**原因:** LVGL 默认假设 `lv_conf.h` 放在 `lvgl` 文件夹的同级目录。本项目将 `lv_conf.h` 放在 `LVGL/` 内部，路径不匹配。

**解决方案 (二选一):**

方案 A — 在编译器 Define 中添加 `LV_CONF_INCLUDE_SIMPLE`，并将 `../LVGL` 加入 Include Path:
```
Define: USE_HAL_DRIVER,STM32H723xx,LV_CONF_INCLUDE_SIMPLE
IncludePath 新增: ..\LVGL
```

方案 B — 将 `lv_conf.h` 移动到 `LVGL` 文件夹的上一级目录 (即项目根目录)。

**验证:** 待实施后重新编译验证。

---

### 问题 4: LVGL 所有源文件路径错误

**现象:** Rebuild 后大量 LVGL 文件报 `cannot open source input file`，包括 core、draw、hal、misc 等模块。

**定位:** `.uvprojx` 中所有 LVGL 文件使用扁平路径 (如 `..\LVGL\src\lv_obj.c`)，但 LVGL v8.3.11 的实际目录结构为分层结构。

**原因:** 工程文件可能是从旧版 LVGL (v7 扁平结构) 模板生成，未适配 v8 的目录结构。

**正确路径映射:**

| 模块 | 错误路径 | 正确路径 |
|------|----------|----------|
| core | `..\LVGL\src\lv_obj.c` | `..\LVGL\src\core\lv_obj.c` |
| draw | `..\LVGL\src\lv_draw.c` | `..\LVGL\src\draw\lv_draw.c` |
| draw/sw | `..\LVGL\src\lv_draw_sw.c` | `..\LVGL\src\draw\sw\lv_draw_sw.c` |
| hal | `..\LVGL\src\lv_hal_disp.c` | `..\LVGL\src\hal\lv_hal_disp.c` |
| misc | `..\LVGL\src\lv_mem.c` | `..\LVGL\src\misc\lv_mem.c` |
| font | `..\LVGL\src\lv_font.c` | `..\LVGL\src\font\lv_font.c` |
| widgets | `..\LVGL\src\lv_btn.c` | `..\LVGL\src\widgets\lv_btn.c` |
| porting | `..\LVGL\lv_port_disp.c` | `..\LVGL\examples\porting\lv_port_disp.c` |

**解决:** 需要重写 `.uvprojx` 中 `LVGL_src` 和 `LVGL_porting` 组的所有文件路径，同时删除不存在的 GPU 后端文件 (SDL、VGLite、PXP、ARM2D 等)。

**验证:** 待完成路径修正后重新编译。

---

### 问题 5: DCache 一致性

**现象:** (潜在问题) DMA 写入帧缓冲后 CPU 读取到旧数据。

**定位:** DMA 直接写入 AXI SRAM，CPU 通过 DCache 读取可能命中缓存中的旧数据。

**原因:** DMA 传输绕过 CPU Cache，导致 Cache 与内存不一致。

**解决:** 每帧 DMA 完成后调用:

```c
SCB_InvalidateDCache_by_Addr((uint32_t *)Camera_Buffer, Display_Width * Display_Height * 2);
```

**验证:** 已在 `Camera_task` 中实现，图像显示正常无花屏。

---

### 问题 6: SPI DMA 导致 DCMI 完全不工作 ⭐ 重要

**现象:** 改用 SPI DMA 传输后，DCMI_FrameState 始终为 0，Camera_Buffer 无任何数据，屏幕无显示。

**定位:** Keil Debug 观察变量，发现 Camera_task 卡死在 `LCD_Init()` → `LCD_Clear()` → `LCD_DMA_Wait()` 中，`DCMI_OV2640_Init()` 从未被执行。

**原因:** STM32H7 的 **DTCM RAM (0x20000000) 不能被 DMA1/DMA2 访问**！Scatter file 将所有 RW/ZI 数据放在 DTCM，导致 `lcd_dma_buf` 在 DTCM 中。DMA1 从 DTCM 读数据时静默失败，`HAL_SPI_TxCpltCallback` 永远不触发，`lcd_dma_busy` 卡在 1，`LCD_DMA_Wait()` 死循环。

**解决:**
1. DMA 缓冲区使用 `__attribute__((section(".ARM.__at_0x24040000")))` 强制放到 AXI SRAM
2. Scatter file 添加 AXI SRAM 执行区域:
```
RW_AXIRAM 0x24040000 0x00010000 {
    *(.ARM.__at_0x24040000)
}
```
3. `LCD_DMA_Wait()` 增加超时保护
4. 添加 `HAL_SPI_ErrorCallback` 防止错误时死锁
5. 添加 SPI1_IRQHandler 处理 HAL DMA 使能的 SPI 错误中断

**验证:** 修改后摄像头采集和 LCD DMA 刷屏均正常工作。

**教训:** 在 STM32H7 上使用 DMA 时，必须确认缓冲区所在的内存区域是否可被 DMA 访问。DTCM 仅 CPU 可访问，DMA 缓冲区必须放在 AXI SRAM、SRAM1~3 等 DMA 可达区域。

---

## 五、版本与发布记录

### V1.0 (初始版本)

**功能:**
- OV2640 摄像头 RGB565 采集 (240×240 裁剪显示)
- DCMI + DMA2 连续传输模式
- ILI9341 LCD SPI1 + DMA1 加速显示 (ping-pong 双缓冲)
- MPU6050 六轴传感器驱动 (软件 I2C)
- FreeRTOS 多任务架构
- LVGL v8.3.11 GUI 框架集成 (进行中)

**已知限制:**
- Keil 工程文件中 LVGL 源文件路径尚未全部修正，需手动更新 `.uvprojx`
- LVGL 编译需要添加 `LV_CONF_INCLUDE_SIMPLE` 宏定义和对应 Include Path
- `MX_FREERTOS_Init()` 和 `vTaskStartScheduler()` 在 `main.c` 中隐式声明 (warning #223)，需添加头文件包含

---

## 六、待办事项

- [ ] 修正 `.uvprojx` 中所有 LVGL 文件路径 (适配 v8 目录结构)
- [ ] 添加 `LV_CONF_INCLUDE_SIMPLE` 编译宏
- [ ] 将 `../LVGL` 加入 Keil Include Path
- [ ] 删除工程中不需要的 GPU 后端文件 (SDL/VGLite/PXP/ARM2D/DMA2D)
- [ ] 完善 LVGL 显示驱动对接，验证 GUI 刷新
- [ ] 优化 SPI DMA 传输 (考虑 16-bit 数据宽度避免 CPU 字节序转换)
- [ ] 添加 MPU6050 数据读取任务
- [ ] 优化 SPI DMA 传输提升刷屏帧率
- [ ] 添加 JPEG 模式支持 (高分辨率传输)





新增：使用定时器定时采集传感器数据，然后产生中断，在中断中切换标志位

通知任务采集传感器数据并写入消息队列
